/**
 * @enum ContactType
 * Represents each type of contact information card.
 */
export enum ContactType {
  Address = 'address',
  Phone = 'phone',
  Email = 'email',
  Availability = 'availability',
}

/**
 * @enum ContactEmail
 * Named email addresses for BC Marine Electronics Inc.
 */
export enum ContactEmail {
  Management = 'md@img-bcme.com',
  Service = 'service@img-bcme.com',
  Sales = 'sales@img-bcme.com',
}

/**
 * @enum EmailLabel
 * Display labels corresponding to ContactEmail values.
 */
export enum EmailLabel {
  Management = 'Management',
  Service = 'Service',
  Sales = 'Sales',
}
