/**
 * @enum Routes
 * Centralized route paths for the application.
 */
export enum Routes {
  Home = '#home',
  About = '#about',
  Services = '#services',
  WhyUs = '#why-us',
  Contact = '#contact',
}

/**
 * @enum ExternalUrls
 * External URLs used in the application.
 */
export enum ExternalUrls {
  GoogleMaps = 'https://maps.google.com/?q=232+6th+Street+New+Westminster+BC+Canada',
  Website = 'https://img-bcme.com',
}

/**
 * @enum AssetPaths
 * Static asset paths in /public folder.
 */
export enum AssetPaths {
  LogoShield = '/logo-shield.png',
  LogoBanner = '/logo-banner.png',
  Favicon = '/favicon.png',
  AppleTouchIcon = '/logo-shield.png',
}
